#!/usr/bin/env bash

set -e
set -o pipefail

BUILD_DIR=$1
CACHE_DIR=$2

cd $CACHE_DIR
ls -la

HAS_CLANG=1
command -v clang++ >/dev/null 2>&1 || { HAS_CLANG=0; }

echo $HAS_CLANG

echo "-----> Doing default"
